import { constants } from 'buffer';
import { Task } from '../tasks/shared/task';

test('Isso é só um teste ok.', () => {
  //const task = new Task()
  //expect(task.lavouroupa).toEqual(true)
  
  const nome = "Neviim"
  expect(nome).toEqual('Neviim')
});
